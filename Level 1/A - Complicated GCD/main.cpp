#include <bits/stdc++.h>
using  namespace std;
#define ll long long
#define ln '\n'
const int MAXN=1e6+4;
void fast() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
}

int main() {
    fast();
    string a,b;
    cin>>a>>b;
    if (a==b)   cout<<a<<ln;
    else
        cout<<1<<ln;
}
